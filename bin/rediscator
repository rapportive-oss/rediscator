#!/usr/bin/env bash

RUBY=$(which ruby)

exec "$RUBY" \
  -rubygems \
  -I"$(dirname $0)/../lib" \
  -e 'require "bundler/setup"; require "rediscator"; $0 = "rediscator"; Rediscator::App.start' \
  "$@"
